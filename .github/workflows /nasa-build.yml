name: NASA API Call

on:
  # push:
  #   paths:
  #     - '**' 
  workflow_dispatch:

jobs:
  nassa-api-call:
    runs-on: ubuntu-latest

    steps:
      - name: echo
        run: echo "echo"
    
      # - name: Call API
      #   run: curl https://api.nasa.gov/planetary/apod?api_key=${{ secrets.DEMO_KEY }} -o output.json

      # - name: Upload output.json
      #   uses: actions/upload-artifact@v4
      #   with:
      #     name: output-json
      #     path: output.json
      
